# nRF54LM20 Radio TX - 裸 Radio + DPPI 8K
# 不使用 ESB 库，直接操作 Radio 寄存器

CONFIG_CLOCK_CONTROL=y
CONFIG_DK_LIBRARY=y
CONFIG_LOG=y
CONFIG_LOG_MODE_DEFERRED=y

# printf 浮点支持（统计打印需要）
CONFIG_REQUIRES_FLOAT_PRINTF=y
